find_package(CycloneDDS QUIET)
if (CycloneDDS_FOUND)
    message(STATUS "CycloneDDS found, building DDS-IPC")
    add_subdirectory(ros2_idl)
    add_subdirectory(dds_ipc)
else ()
    message(STATUS "CycloneDDS not found, not building DDS-IPC")
endif ()

find_package(iceoryx_posh 2.0.3 QUIET)
if (iceoryx_posh_FOUND)
    message(STATUS "iceoryx found, building SHM-IPC")
    add_subdirectory(shm_ipc)
else ()
    message(STATUS "iceoryx not found, not building SHM-IPC")
endif ()